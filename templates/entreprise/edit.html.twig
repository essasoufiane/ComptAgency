{% extends 'base.html.twig' %}

{% block title %}Éditer Entreprise
{% endblock %}

{% block body %}
	<div class="container">
		<h1 class="titleContact py-3">Éditer Entreprise</h1>

		{{ include('entreprise/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
			<a class="btn btn-primary my-3" href="{{ path('app_entreprise_index') }}">Retour à la liste</a>

			{{ include('entreprise/_delete_form.html.twig') }}
	</div>
{% endblock %}
